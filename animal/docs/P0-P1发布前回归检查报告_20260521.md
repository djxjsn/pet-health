# P0-P1 发布前回归检查报告（2026-05-21）

## 1. 检查目标

本次检查用于验证 P0 稳定性修复与 P1 架构收敛改动在发布前的可用性，重点覆盖：

- Chat 主链路默认切换到 AgentV2（异常自动回退 V1）
- 紧急场景前置策略（Emergency Gate）
- WebSocket 非阻塞处理（线程池 + 超时）
- 编排信息透传（`orchestration`）与统计接口
- Mongo 会话消息计数累加逻辑

## 2. 本次涉及改动文件

- `animal/src/agents/agent_v2.py`
- `animal/src/agents/pet_health_agent.py`
- `animal/src/api/v1/endpoints/chat.py`
- `animal/src/api/v1/endpoints/ws_chat.py`
- `animal/src/repositories/mongo_repositories.py`
- `animal/src/schemas/conversation.py`
- `animal/tests/test_chat_api.py`

## 3. 执行命令清单

### 3.1 语法编译检查

```bash
python -m py_compile \
  "animal/src/agents/agent_v2.py" \
  "animal/src/agents/pet_health_agent.py" \
  "animal/src/api/v1/endpoints/chat.py" \
  "animal/src/api/v1/endpoints/ws_chat.py" \
  "animal/src/repositories/mongo_repositories.py" \
  "animal/src/schemas/conversation.py" \
  "animal/tests/test_chat_api.py"
```

结果：通过（无语法错误）。

### 3.2 核心回归测试

```bash
python -m pytest "animal/tests/test_chat_api.py" -k \
"chat_endpoint or emergency_orchestration or orchestration_stats_endpoint or fallback_to_v1_when_v2_fails or message_repository_create_increments_message_count" -q
```

结果：通过。

- 总计选择用例：5
- 通过：5
- 失败：0

## 4. 关键验证点与结论

### 4.1 Chat 编排链路

- 验证点：
  - `/api/v1/chat` 返回 `orchestration` 字段
  - 正常场景 `engine=v2`
  - V2 异常场景自动回退 `engine=v1` 且 `fallback=true`
- 结论：通过。

### 4.2 紧急场景前置策略

- 验证点：
  - 紧急关键词命中时走 `engine=emergency_gate`
  - 返回急诊导向响应，不进入常规工具编排
- 结论：通过。

### 4.3 编排统计能力

- 验证点：
  - `/api/v1/chat/orchestration/stats` 可返回计数与比例
  - 按 `v2/v1/emergency_gate` 分桶统计可用
- 结论：通过。

### 4.4 Mongo 消息计数更新

- 验证点：
  - `MessageRepository.create()` 后会话 `message_count` 正确累加
- 结论：通过。

## 5. 发布建议

当前版本满足 P0/P1 目标，建议可进入联调或灰度阶段。

建议上线后重点观察以下指标：

- `chat_orchestration` 日志中的 `engine` 分布（`v2` 占比）
- `fallback` 比例（反映 V2 稳定性）
- `emergency_hit` 命中数（反映高风险查询流量）
- `/api/v1/chat/orchestration/stats` 中的 `fallback_rate` 与 `emergency_hit_rate`

## 6. 已知非阻塞项

- 运行日志存在 `datetime.utcnow()` 的 deprecation warning（不影响当前功能正确性，建议后续统一迁移为 timezone-aware 时间）。
- FastAPI `on_event` 弃用提醒为历史技术债，不影响本次 P0/P1 发布验证。

