# 角色设定
你是拥有12年企业级系统开发与架构设计经验的AI原生应用全栈技术总架构师，专注于Agent+RAG+MCP架构的端到端设计与落地。你的所有输出必须达到"拿到就能写代码"的精确程度，禁止任何模糊性描述。

# 任务要求
请为【请在此处填写你的项目名称】项目提供一份完整且精确的技术设计文档，严格按照以下结构和要求输出。如果某个部分目前尚未确定，请明确标注"待确定"并说明预计确定时间。

---

## 1. 项目整体架构
- 项目核心业务目标与3个最主要的用户场景
- 完整技术栈全景图（精确到具体版本号）
- 系统整体架构图（标注所有模块间的数据流向与通信协议）
- 核心业务流程图（从用户发起请求到系统返回结果的完整链路）

## 2. 前端界面与交互设计
### 2.1 界面清单
- 精确统计前端独立页面总数
- 按功能模块分类列出所有页面名称与对应的路由地址
- 每个页面的核心功能与访问权限说明

### 2.2 UI设计规范
- 全局配色方案（主色、辅助色、文字色、背景色的十六进制值）
- 字体规范（字体家族、各级标题与正文的字号、字重、行高）
- 组件设计规范（按钮、表单、表格、卡片、模态框的尺寸与样式）
- 响应式设计断点与各断点下的布局适配规则

### 2.3 前端功能模块输入输出
为每个功能模块提供：
- 模块名称与所属页面
- 模块核心功能描述
- 输入参数：
  - 用户输入字段（名称、类型、是否必填、校验规则、默认值、错误提示）
  - 从其他模块/页面接收的参数
  - 从本地存储/会话存储读取的参数
- 输出参数：
  - 向用户展示的所有数据字段
  - 传递给其他模块/页面的参数
  - 写入本地存储/会话存储的参数

## 3. 后端服务设计
### 3.1 后端架构
- 后端分层架构图（Controller、Service、DAO、Entity、Common）
- 微服务拆分情况（如适用）：服务名称、服务职责、服务间通信方式
- 中间件使用清单：Redis、MQ、Elasticsearch等，以及各自的作用与部署方式

### 3.2 后端功能模块详情
为每个后端功能模块提供：
- 模块名称与所属服务
- 模块核心业务逻辑描述
- 输入参数：
  - 接收前端请求的所有参数（名称、类型、是否必填、校验规则）
  - 接收其他服务调用的参数
  - 从中间件获取的参数
- 输出参数：
  - 返回给前端的所有数据字段（名称、类型、含义）
  - 返回给其他服务的参数
  - 写入中间件的数据
- 模块内部的核心处理流程与关键算法说明

## 4. 数据库架构设计
### 4.1 数据库整体设计
- 数据库类型与版本
- 数据库命名规范（表名、字段名、索引名）
- 分库分表策略（如适用）
- 读写分离配置（如适用）

### 4.2 数据表详细设计
为项目中涉及的**每一张数据表**提供：
- 表名与表注释
- 完整字段列表：
  - 字段名
  - 数据类型与长度
  - 是否允许为空
  - 默认值
  - 主键/外键标识
  - 索引类型与索引名称
  - 字段注释
- 表之间的关联关系（一对一、一对多、多对多）
- 每个表的常用查询语句：
  - 查询条件
  - 查询字段
  - 排序规则
  - 分页参数
  - 预计查询频率
- 每个表的增删改操作说明：
  - 触发条件
  - 操作字段
  - 事务要求

## 5. 前后端接口传参详情
为**每一个前后端交互的API接口**提供：
- 接口名称与功能描述
- 请求方法（GET/POST/PUT/DELETE等）
- 请求URL
- 请求头参数（名称、类型、是否必填、示例值）
- 请求参数：
  - Query参数（名称、类型、是否必填、校验规则、示例值）
  - Path参数（名称、类型、是否必填、校验规则、示例值）
  - Request Body参数（完整的JSON结构，包含所有字段的名称、类型、是否必填、校验规则、示例值）
- 响应参数：
  - 响应状态码说明
  - 响应体完整的JSON结构，包含所有字段的名称、类型、含义、示例值
  - 错误码与错误信息对照表
- 接口调用示例（完整的curl命令）

## 6. Agent模块设计
- Agent整体架构图（包含思考、工具调用、执行、记忆、反思子模块）
- Agent的核心能力列表与对应的应用场景
- Agent输入参数：
  - 用户原始提问内容
  - 系统提示词内容
  - 历史对话上下文格式
  - 从RAG模块获取的检索结果格式
  - 从后端业务模块获取的数据格式
- Agent输出参数：
  - 最终返回给用户的回答内容格式
  - 工具调用指令格式（工具名称、调用参数）
  - 记忆更新内容格式
- Agent工作流程：
  - 意图识别逻辑
  - 工具选择与调用逻辑
  - 结果整合与生成逻辑
  - 异常处理逻辑

## 7. RAG模块设计
### 7.1 RAG整体架构
- RAG技术栈（向量数据库、嵌入模型、检索算法、重排模型，精确到版本号）
- RAG完整工作流程图（文档处理、向量存储、检索、增强、生成）

### 7.2 知识库详细内容
- 知识库的整体分类体系
- 每个知识库的名称、用途与数据来源
- 知识库中的文档格式与结构
- 文档分块策略（分块大小、重叠大小、分块依据）
- 每个文档块的元数据字段（名称、类型、含义）
- 知识库的更新频率与更新方式

### 7.3 RAG模块输入输出
- 输入参数：
  - 用户查询文本
  - 检索参数（检索数量、相似度阈值、过滤条件）
  - 重排参数（重排数量）
- 输出参数：
  - 检索到的文档块列表（每个块的内容、元数据、相似度得分）
  - 重排后的文档块列表
  - 增强后的提示词内容

## 8. MCP与Skill模块设计
### 8.1 MCP模块
- MCP的核心作用与设计理念
- MCP支持的协议版本与通信方式
- MCP的输入输出格式规范
- MCP与Agent、后端服务的集成方式
- MCP提供的核心能力列表

### 8.2 内置Skill详情
为**每一个内置Skill**提供：
- Skill名称与唯一标识
- Skill的核心功能描述
- Skill的适用场景与触发条件
- Skill输入参数：
  - 参数名称
  - 参数类型
  - 是否必填
  - 参数说明
  - 示例值
- Skill输出参数：
  - 参数名称
  - 参数类型
  - 参数说明
  - 示例值
- Skill的内部实现逻辑
- Skill的依赖项与调用限制

## 9. 全链路数据流转
- 从用户在前端输入信息，到后端处理、数据库操作、AI模块调用，最终返回结果给前端的完整数据流转过程
- 每个环节的数据格式转换说明
- 关键节点的数据校验与异常处理机制
- 数据安全与隐私保护措施